let list= li
document.querySelectorAll(list)[0];
// the below tag will make the output to be invisible
list.classList.Add("invisible");
// the below tag will remove the output from been invisible
list.classList.Remove("invisible")
// the below tag will change the output from pray to praying
list.texrContent="praying"
// the below tag will change the complete paragraph
list.innerhtml="praying"

var img= pictures;